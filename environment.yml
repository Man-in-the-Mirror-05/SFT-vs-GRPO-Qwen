name: sft
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - cudatoolkit=11.8
  - pip:
    - torch>=2.0.0
    - torchvision
    - torchaudio
    - transformers>=4.40.0
    - datasets>=2.14.0
    - accelerate>=0.25.0
    - peft>=0.7.0
    - trl>=0.8.0
    - bitsandbytes>=0.41.0
    - tensorboard
    - numpy
    - pandas
    - scikit-learn
    - tqdm
    - pyyaml
    - colorama
